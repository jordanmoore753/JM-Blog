<% provide(:title, 'Posts') %>
<div class="container">
  <div class="content">
    <h1>Posts</h1>
  </div>
  <%= will_paginate(@posts, renderer: BulmaPagination::Rails) %>
  <%= render 'posts' %>
</div>