<% provide(:title, @post.title) %>

<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<h2>Comments</h2>
<ul>
  <% @comments.each do |comment| %>
    <li><%= comment.body %>, <span>written by <%= @authors["#{comment.id}"] %></span></li>
  <% end %>
</ul>